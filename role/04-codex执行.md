按当前项目规则和当前任务流程继续。先读真源和当前任务包，严格按当前任务允许范围执行，并在完成后回写运行态文件。

只修改当前任务允许修改的文件，并必须回写 review-notes.md 和 task-status.md。

执行前必须先检查最近一次 codex 计划结论：
- 仅当结论为 ACCEPT 时，才允许进入执行
- 若结论为 REVISE 或 REJECT，则禁止执行，必须先回到发布/计划环节修订任务