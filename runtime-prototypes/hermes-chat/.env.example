# Hermes API Server — Vite 代理目标（仅 vite.config.ts 使用，前端业务代码不得直接访问）
VITE_HERMES_TARGET=http://127.0.0.1:8056

# 浏览器请求前缀，由 Vite dev proxy 转发到 Hermes
VITE_HERMES_PROXY_PREFIX=/hermes-api

# 默认 API Key（本地开发原型，生产环境须由后端网关注入）
VITE_HERMES_API_KEY=hermes-dev-key-2026
